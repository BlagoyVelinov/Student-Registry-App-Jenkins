pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                bat 'Hello World'
            }
        }
    }
    // post {
    //     always {
    // // actions to perform afterthe pipeline runs new test
    //    }
    // }
}
