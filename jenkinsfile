pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
    // Commands to build
            }
        }
    }
    // post {
    //     always {
    // // actions to perform afterthe pipeline runs new test
    //    }
    // }
}
