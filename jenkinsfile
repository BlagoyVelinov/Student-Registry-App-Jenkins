pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Hello World'
            }
        }
    }
    // post {
    //     always {
    // // actions to perform afterthe pipeline runs 
    //    }
    // }
}
